flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1

# Shared ML
torch==2.3.0
torchvision==0.18.0
pillow==10.3.0
numpy==1.26.4

# Obj 1 — Alt-text (BLIP via HuggingFace)
transformers==4.41.2

# Obj 2 — Sentiment (ABSA + ASE via HuggingFace)
# transformers already above

# Obj 3 — Recommendations (ResNet-50 cosine similarity)
scikit-learn==1.5.0

# Obj 4 — Categorization (EfficientNet-B0 fine-tuned)
# torch + torchvision already above

# Obj 5 — Chatbot (RAG + GPT-2 base, fallback: Groq/Ollama)
sentence-transformers==2.7.0
requests==2.32.3

# Obj 6 — Defect detection (OpenCV heuristics)
opencv-python-headless==4.10.0.82
